
any data compare
  identical scalar types
    any_data_bdouble
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_bfloat
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_number
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_blob
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_raw
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_clob
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_char
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_varchar
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_varchar2
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_date
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_timestamp
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_timestamp_tz
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
    any_data_timestamp_ltz
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
      NULL values comparison
        returns NULL if self holds NULL value
        returns NULL if compared holds NULL value
        returns 0 if both holds NULL value
  scalar types from the same family
    compares any_data_bdouble with any_data_bfloat
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_bdouble with any_data_number
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_bfloat with any_data_bdouble
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_bfloat with any_data_number
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_number with any_data_bdouble
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_number with any_data_bfloat
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_blob with any_data_raw
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_raw with any_data_blob
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_char with any_data_varchar
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_char with any_data_varchar2
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_char with any_data_clob
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_varchar with any_data_char
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_varchar with any_data_varchar2
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_varchar with any_data_clob
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_varchar2 with any_data_char
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_varchar2 with any_data_varchar
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_varchar2 with any_data_clob
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_clob with any_data_char
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_clob with any_data_varchar
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_clob with any_data_varchar2
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_date with any_data_timestamp
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_date with any_data_timestamp_ltz
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_date with any_data_timestamp_tz
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp with any_data_date
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp with any_data_timestamp_ltz
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp with any_data_timestamp_tz
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp_ltz with any_data_date
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp_ltz with any_data_timestamp
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp_ltz with any_data_timestamp_tz
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp_tz with any_data_date
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp_tz with any_data_timestamp
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
    compares any_data_timestamp_tz with any_data_timestamp_ltz
      non null values comparison
        returns 0 if both hold equal values
        returns -1 if self value is bigger
        returns 1 if compared value is bigger
  scalar types from different families
    returns NULL if data is of different families
  complex types
    any_data_attribute
      returns 0 for identical attributes
      returns 0 regardless of attribute name case
      returns 1 for attribute name greater than compared
      returns -1 for attribute name less than compared
      returns 1 if attribute data is less
      returns -1 if attribute data is greater
      returns NULL when one of attributes data is NULL
      returns 0 when both attributes data is NULL
      returns NULL when attribute contain data from different families
    any_data_collection
      returns 0 for identical collections
      returns 0 for collections containing the same elements regardless of collection type name
      returns 1 of compared collection is less
      returns -1 of compared collection is greater
      returns 1 if collections are of equal size but compared collections element is less
      returns -1 if collections are of equal size but compared collections element is greater
      returns NULL when one of collections data is NULL
      returns 0 when both collections data is NULL
      returns 0 when both collections data is contain NULL elements
      returns NULL when collections contain data from different families
      returns 0 when collections contain data from common family
    any_data_object
      returns 0 for identical objects
      returns 0 for objects regardless of object type name
      returns 1 of compared object is less
      returns -1 of compared object is greater
      returns 1 if objects are of equal size but compared objects element is less
      returns -1 if objects are of equal size but compared objects element is greater
      returns NULL when one of objects data is NULL
      returns 0 when both objects data is NULL
      returns 0 when both objects data is contain NULL elements
      returns NULL when objects contain data from different families
      returns 0 when objects contain data from common family

any data equality comparison
  using equals method
    non NULL comparison
      returns true for identical data
      returns false for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value
  using eq method
    non NULL comparison
      returns true for identical data
      returns false for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value
  using "= " method
    non NULL comparison
      returns true for identical data
      returns false for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value

any data non equality comparison
  using not_equals method
    non NULL comparison
      returns false for identical data
      returns true for when comparing to lesser value
      returns true for when comparing to greater value
      returns true for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns true when comparing to a NULL value
  using neq method
    non NULL comparison
      returns false for identical data
      returns true for when comparing to lesser value
      returns true for when comparing to greater value
      returns true for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns true when comparing to a NULL value
  using "!=" method
    non NULL comparison
      returns false for identical data
      returns true for when comparing to lesser value
      returns true for when comparing to greater value
      returns true for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns true when comparing to a NULL value

any data greater equal to comparison
  using greater_equal_to method
    non NULL comparison
      returns true for identical data
      returns true for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value
  using ge method
    non NULL comparison
      returns true for identical data
      returns true for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value
  using ">=" method
    non NULL comparison
      returns true for identical data
      returns true for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value

any data greater than comparison
  using greater_than method
    non NULL comparison
      returns false for identical data
      returns true for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns false when comparing to a NULL value
  using gt method
    non NULL comparison
      returns false for identical data
      returns true for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns false when comparing to a NULL value
  using "> " method
    non NULL comparison
      returns false for identical data
      returns true for when comparing to lesser value
      returns false for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns false when comparing to a NULL value

any data less equal to comparison
  using less_equal_to method
    non NULL comparison
      returns true for identical data
      returns false for when comparing to lesser value
      returns true for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value
  using le method
    non NULL comparison
      returns true for identical data
      returns false for when comparing to lesser value
      returns true for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value
  using "<=" method
    non NULL comparison
      returns true for identical data
      returns false for when comparing to lesser value
      returns true for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns true when comparing NULL to NULL
      returns false when comparing to a NULL value

any data less than comparison
  using less_than method
    non NULL comparison
      returns false for identical data
      returns false for when comparing to lesser value
      returns true for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns false when comparing to a NULL value
  using lt method
    non NULL comparison
      returns false for identical data
      returns false for when comparing to lesser value
      returns true for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns false when comparing to a NULL value
  using "< " method
    non NULL comparison
      returns false for identical data
      returns false for when comparing to lesser value
      returns true for when comparing to greater value
      returns false for data types of different families
    NULL comparison
      returns false when comparing NULL to NULL
      returns false when comparing to a NULL value

library version
  any_data.get_version
    returns current project version number
  any_data_builder.get_version
    returns current project version number
  any_type_mapper.get_version
    returns current project version number
  any_data_const.get_version
    returns current project version number
  any_data_formatter.get_version
    returns current project version number

any_data construction
  any_data_number
    creates instance of object using full constructor
    creates instance of object using data value only
  any_data_blob
    creates instance of object using full constructor
    creates instance of object using data value only
  any_data_char
    creates instance of object using full constructor
    creates instance of object using data value only
  any_data_varchar
    creates instance of object using full constructor
    creates instance of object using data value only
  any_data_varchar2
    creates instance of object using full constructor
    creates instance of object using data value only
  any_data_clob
    creates instance of object using full constructor
    creates instance of object using data value only
  any_data_date
    creates instance of object using full constructor
    creates instance of object using data value only
  any_data_collection
    creates instance of object using full constructor
    creates instance of object using data values and type name
  any_data_object
    creates instance of object using full constructor
    creates instance of object using data values and type name

Build any_data object from anydata
  build-in Oracle scalar types
    converts a binary_double into a string representation
    converts a binary_float into a string representation
    converts a blob into a string representation
    converts a raw into a string representation
    converts a clob into a string representation
    converts a char into a string representation
    converts a date into a string representation
    converts a integer into a string representation
    converts a interval day to second into a string representation
    converts a interval year to month into a string representation
    converts a number into a string representation
    converts a varchar into a string representation
    converts a varchar2 into a string representation
  user defined object type
    converts an object into string representation
    converts nested object into string representation
    converts an object containing a collection into string representation
  defined collection types
    converts collection of primitives into string representation
    converts collection of objects into string representation
    converts collection of collections into string representation
    converts collection of objects with collections into string representation
  sub-typed objects
    converts collection of super-type containing sub-types into string representation
    converts sub-typed object within an object

Indent lines in a string
  returns a single line string indented
  returns NULL for NULL input
  returns a string with lines indented

Indent elements of an array
  returns a single line string indented
  returns NULL for NULL input
  returns a string with lines indented

Finished in 4.67 seconds (files took 7.07 seconds to load)
365 examples, 0 failures

